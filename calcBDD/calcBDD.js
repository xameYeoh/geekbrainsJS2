const sum = (a, b) => {
    return a + b;
}

const sub = (a, b) => {
    return a - b;
}

const divide = (a, b) => {
    return a / b;
}

const mul = (a, b) => {
    return a * b;
}

module.exports = {
    sum: sum,
    sub: sub,
    divide: divide,
    mul: mul,
}